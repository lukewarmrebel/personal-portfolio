---
context: phase-1-wave1-complete
phase: 1
status: wave1-done-needs-git-fix
last_updated: 2026-05-23
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

## CRITICAL: site/ is a nested git repo (gitlink)

`site/` has its own `.git` directory from `create-next-app`. When we ran `git add site/`, git added it as a submodule reference (gitlink at commit `abfea37`), NOT as tracked files. The outer repo only tracks a pointer, not the actual source files.

**Fix before doing anything else:**
```powershell
cd "E:\Projects\Portfolio"
git rm --cached site
Remove-Item -Recurse -Force "site\.git"
git add site/
git commit -m "fix: track site/ as regular files (remove nested git repo)"
```

This is safe — site/.git has no meaningful history worth preserving (create-next-app default init only). All real changes are in E:\Projects\Portfolio git history.

# Completed Work (Wave 1 — all code changes applied)

- **P01** ✓ `site/src/app/page.tsx` — Skills, Education, Contact wired in after Projects
- **P02** ✓ `site/src/app/layout.tsx` — metadataBase + openGraph.images with /pranav-formal.jpg
- **P03** ✓ `site/src/components/icons.tsx` — created with GithubIcon/LinkedinIcon (optional className prop)
- **P03** ✓ `site/src/components/Hero.tsx` — imports from icons.tsx, uses className="w-5 h-5"
- **P03** ✓ `site/src/components/InfoPanel.tsx` — imports from icons.tsx, uses edu.color (removed eduGradients)
- **P03** ✓ `site/src/components/Contact.tsx` — imports from icons.tsx
- **P03** ✓ `site/src/data/portfolio.ts` — featuredSkills added, resumeUrl = /resume.pdf
- **P03** ✓ `site/src/components/About.tsx` — uses personal.featuredSkills
- **P03** ✓ `site/public/` — 5 scaffold SVGs deleted, resume.pdf present
- **Git** ⚠ site/ tracked as gitlink — needs the fix above

# Remaining Work (Wave 2)

**P04: Build Verification + Vercel Deploy** (autonomous: false — needs human)

After fixing git:
1. `cd site && npm run build` — verify exits 0
2. Start dev server, check all 6 Navbar links scroll correctly
3. Check /resume.pdf loads
4. Mobile QA at 375px
5. Deploy to Vercel: `vercel --prod` from site/
6. Update metadataBase URL in layout.tsx with real Vercel URL, redeploy

# Next Action

1. Fix git (nested repo issue above)  
2. Run `npm run build` from `E:\Projects\Portfolio\site`
3. Start dev server and verify the site visually
4. Deploy to Vercel
