---
context: phase
phase: 01-fix-and-polish
task: 9
total_tasks: 10
status: in_progress
last_updated: 2026-05-24
---

# Resume Point — Deploy to Vercel

_No blocking constraints. Clean state. Only deploy step remains._

<current_state>
All code is complete and committed. Site loads cleanly (200 OK) in dev. Skills.tsx crash fixed this session (colors map was missing AI & Agents entry). Only task remaining: production deploy + update metadataBase URL.
</current_state>

<completed_work>

- Dark mode: FOUC-free init script in layout.tsx, globals.css variable overrides, Navbar Sun/Moon toggle (localStorage-persisted)
- Dark mode bug fix: Turbopack dropped all CSS after a Unicode box-drawing comment (`──`) — replaced with plain ASCII
- CareVault project: 14 screenshots in public/carevault/, animated carousel card, dedicated /projects/carevault page with thumbnail strip
- Project detail page: /projects/[slug] (client component, useParams, Framer Motion entrance, full carousel + thumbnail strip)
- Explore button conditional: only shown on cards with `screenshots` array
- Projects overhaul: Sentinel + 6 screenshots, Sentinel Eval Teardown (PDF), InsurAgents, ResuAgent, Car Damage Annotation added; Motor Claims Fraud + AI Vehicle Inspection removed
- AI & Agents skills category: 12 skills added as first category in skills section
- Skills.tsx crash fix: added "AI & Agents" violet color entry to the colors map (was missing, caused c.header undefined crash)
</completed_work>

<remaining_work>

- **Deploy**: `cd E:\Projects\Portfolio\site && vercel --prod` (user must run — auto-mode blocked it)
- **Post-deploy**: update `metadataBase` in `site/src/app/layout.tsx` line 8 with real Vercel URL, then redeploy
</remaining_work>

<decisions_made>

- Turbopack silently drops all CSS after a Unicode character in a comment — use ASCII-only comments in globals.css
- Screenshot carousel uses direction-aware AnimatePresence slide (Framer Motion already in project)
- Project detail page is a client component using `useParams()` — simpler than server/client split for a portfolio
- `projectSlug()` utility in portfolio.ts derives URL slug from title — no need for explicit slug field on every project
- Sentinel teardown PDF copied to public/sentinel_teardown.pdf and linked as project artifact
- AI & Agents is the first skills category — strongest differentiator for PM role
- **IMPORTANT**: Skills.tsx colors map must include every category key in portfolio.ts skills object — adding a skills category without a corresponding colors entry crashes the Skills component with "Cannot read properties of undefined (reading 'header')"
</decisions_made>

<blockers>

- Vercel deploy needs manual run or explicit user authorization (auto-mode classifier blocks it)
</blockers>

## Required Reading
1. `site/src/app/layout.tsx` — line 8 has placeholder metadataBase URL to update after deploy
2. `site/src/components/Skills.tsx` — if adding any new skills category to portfolio.ts, add a matching color entry here too

## Infrastructure State
- Dev server: `npm run dev` from `site/` — runs on localhost:3000
- Site returns 200 OK after Skills.tsx fix
- All screenshots are in `site/public/` (carevault/, sentinel/, resumeagent/, cardamage/)
- sentinel_teardown.pdf in site/public/

## Key File Locations
- Portfolio data: `site/src/data/portfolio.ts` — projects array + skills object + projectSlug()
- Skills colors map: `site/src/components/Skills.tsx` — must stay in sync with skills categories
- Project detail page: `site/src/app/projects/[slug]/page.tsx`
- Projects card component: `site/src/components/Projects.tsx`
- Dark mode CSS: `site/src/app/globals.css` (rules after `.chip:hover`)

<next_action>
Start with: `cd E:\Projects\Portfolio\site && vercel --prod`. After getting the live URL, edit `site/src/app/layout.tsx` line 8 — replace `https://pranav-portfolio.vercel.app` with the real URL — then run `vercel --prod` once more.
</next_action>
